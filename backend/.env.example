KMS_MASTER_KEY=BASE64_32_BYTES_GENERATED_BY_NODE
PG_ROOT_USER=postgres
PG_ROOT_PASSWORD=postgres
PG_HOST=db
PG_PORT=5432
PORT=3000
KEYCLOAK_HOST=http://keycloak:8080
KEYCLOAK_CLIENT_ID=cfo-client
KEYCLOAK_REALM=master
OPENAI_API_KEY=
PORT=3000

# Postgres root connection for provisioning tenant DBs
PG_ROOT_USER=postgres
PG_ROOT_PASSWORD=postgres
PG_HOST=db
PG_PORT=5432

# Redis for rate limiting and caching (optional; falls back to in-memory if not set)
# For Docker: REDIS_HOST=redis
# For local development without Redis: leave empty
REDIS_HOST=redis
REDIS_PORT=6379

# KMS_MASTER_KEY must be base64 of 32 bytes. Example generator:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
KMS_MASTER_KEY=
